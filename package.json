{
  "name": "live-voice-translator",
  "version": "1.0.0",
  "private": true,
  "description": "Mobile-first AI voice translation web app.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check service-worker.js"
  },
  "engines": {
    "node": ">=20"
  }
}
